{
  "name": "@subspace/search-tree",
  "description": "",
  "keywords": [],
  "version": "0.0.1",
  "homepage": "https://github.com/subspace/search-tree",
  "author": "Nazar Mokrynskyi <nazar@mokrynskyi.com>",
  "repository": {
    "type": "git",
    "url": "git://github.com/subspace/search-tree.git"
  },
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run lint && npm run compile",
    "clean": "rm -rf dist",
    "compile": "tsc -b",
    "lint": "tslint --project .",
    "test": "npm run lint && ts-node node_modules/.bin/tape tests/**/*.ts"
  },
  "dependencies": {
    "@types/node": "^12.0.10"
  },
  "devDependencies": {
    "@types/tape": "^4.2.33",
    "tape": "^4.10.2",
    "ts-node": "^8.3.0",
    "tslint": "^5.18.0",
    "typescript": "^3.5.2"
  }
}
